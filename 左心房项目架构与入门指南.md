# 左心房项目架构与入门指南

该左心房项目是基于 **BACFormer 边界感知 Transformer** 的医学影像分割系统，专门针对左心房医学影像实现自动分割，同时提供了模型训练模块和可视化交互 GUI 界面，覆盖了从模型训练到落地使用的完整流程。

---

## 一、项目整体架构

整个项目按照功能划分为 5 大核心模块，各模块职责清晰，文件分工明确：

### 1. 核心算法模型模块

这是项目的算法核心，实现了专门适配左心房分割任务的定制化分割模型：

|文件|职责说明|
|---|---|
|`BACFormer.py`|**项目核心文件**，实现了 BACFormer 边界感知分割模型，采用 ResNet18 作为编码器 + 自定义上采样解码器的编码器 - 解码器结构，针对单通道医学影像做了专门优化，支持左心房的二分类分割|
|`segformer.py`|基础 SegFormer 模型实现，作为项目的基础模型参考|
|`model.py`|模型的通用封装逻辑，提供模型的通用加载、配置能力|
### 2. 数据处理模块

负责左心房数据集的加载、预处理，将原始医学影像转换为模型可处理的格式：

|文件|职责说明|
|---|---|
|`dataset_left_atrium.py`|左心房专属数据集加载类，负责原始影像的读取、灰度归一化、3D 体积转 2D 切片、数据增强等预处理逻辑|
### 3. 训练与评估模块

负责模型的训练、验证、测试全流程管理，以及模型效果的评估：

|文件|职责说明|
|---|---|
|`train.py`|训练流程入口文件，负责解析训练参数、加载数据集、初始化模型与训练器，启动训练流程|
|`test.py`|测试流程入口文件，加载训练好的模型，在测试集上完成推理与效果评估，可保存预测结果|
|`trainer.py`|训练逻辑封装，实现了完整的训练循环、优化器配置、损失计算、周期验证、模型权重保存等核心训练逻辑|
|`metrics.py`/`metric_utils.py`|分割任务评估指标实现，支持 Dice 系数、IoU 等医学分割常用的评估指标，用来量化模型的分割效果|
### 4. 可视化 GUI 交互模块

提供图形化交互界面，无需命令行即可完成影像的加载、分割与结果查看：

|文件|职责说明|
|---|---|
|`main.py`|GUI 程序启动入口，初始化 Qt 应用，加载主窗口，完成高 DPI 适配等初始化工作|
|`main_window.py`|GUI 主窗口实现，包含界面交互逻辑、模型推理调用、分割结果可视化、左心房参数测量等功能|
|`start.bat`/`start.sh`|一键启动脚本，Windows 系统双击 bat 文件、Linux/Mac 系统运行 sh 文件即可直接启动 GUI，无需手动输入命令|
### 5. 通用工具模块

提供整个项目通用的工具能力，被各个模块复用：

|文件|职责说明|
|---|---|
|`utils.py`|通用工具函数集合，包含图像处理、路径处理、日志工具等通用能力|
---

## 二、入门：从哪个核心文件开始看？

根据你的不同目标，推荐从不同的核心文件入手：

### 1. 如果你想快速运行项目，体验 GUI 界面

推荐从 **`main.py`** 开始看，这是 GUI 程序的启动入口：

- 它会初始化 PyQt 应用，加载训练好的模型权重，启动左心房测量的主窗口

- 你也可以直接运行 `start.bat`（Windows）或 `start.sh`（Linux/Mac），一键启动界面，无需配置命令，直接加载影像查看分割效果

### 2. 如果你想复现模型训练，了解训练流程

推荐从 **`train.py`** 开始看，这是训练流程的入口：

- 它会先解析训练的命令行参数（比如数据集路径、学习率、批次大小等）

- 然后加载左心房数据集、初始化 BACFormer 模型，最后创建 Trainer 启动训练

- 顺着这个文件，你可以逐步理清：数据是怎么加载的、模型是怎么初始化的、训练流程是怎么运转的，之后可以再深入看 `trainer.py` 了解具体的训练逻辑，`dataset_left_atrium.py` 了解数据预处理细节

### 3. 如果你想了解项目的核心算法，研究 BACFormer 模型

推荐直接从 **`BACFormer.py`** 开始看，这是整个项目的算法核心：

- 这个文件实现了论文对应的边界感知分割模型，包含了完整的模型结构定义、前向传播逻辑

- 里面详细实现了针对医学影像的编码器 - 解码器结构，以及跳跃连接的特征融合逻辑，是这个项目最核心的创新点实现

---

## 三、快速上手参考

如果需要快速配置运行，可以先查看项目的 `README.md`，里面包含了：

- 环境依赖配置（Python3.9、PyTorch1.13 等）

- 训练、测试的命令示例

- 数据集的准备说明
